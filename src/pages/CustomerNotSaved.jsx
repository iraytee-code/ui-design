import React, { Fragment } from "react";
import { NavBar, CustomerDetailsBlank } from "../components";

const CustomerNotSaved = () => {
  return (
    <Fragment>
      <NavBar />
      <CustomerDetailsBlank />
    </Fragment>
  );
};

export default CustomerNotSaved;
